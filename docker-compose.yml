version: "3.9"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: onc
      POSTGRES_PASSWORD: onc
      POSTGRES_DB: onc
    ports: ["5432:5432"]
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
